# 服务配置
server:
  port: 8080
  mode: debug

# AI配置
ai:
  # OpenAI兼容 API 地址
  base_url: "https://api.deepseek.com"
  # API密钥（MVP阶段使用环境变量）
  model: "deepseek-chat"
  temperature: 0.7
  max_tokens: 2000
  # 超时设置(秒)
  timeout: 120

# Redis配置
redis:
  addr: "localhost:6379"
  password: ""
  db: 0
